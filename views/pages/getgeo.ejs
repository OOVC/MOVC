<!DOCTYPE html>
<% 
let options = [
    {
        title:__("country-id"),
        id:"idc",
        required:true,
        type:"many",
        types:arr
    }
]
%>

<html>
    <head>
        <%- include("../components/bootstrap") %> 
        <%- include("../components/head") %> 
        <meta name="description" content="Хочешь отредактировать территорию своей страны тогда тебе сюда."/>
        <title>Редактировать территорию страны</title>
    </head>
    <body>
        <%- include("../components/menu") %>
        <div class="container">
            <div class="row text-center">
                <%- include("../components/cuads") %>
            </div> 
            <form action="/geojs" method="GET">
                <%- include("../components/form", {options}) %> 
                <div class="row m-5">
                    <input type="submit" value="<%=__("send")%>" id="send" class="btn btn-dark">
                </div>
            </form>
        </div>
    </body>
</html>